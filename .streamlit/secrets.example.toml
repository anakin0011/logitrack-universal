# Copiá este archivo como secrets.toml y completá con tus datos reales.
# En Streamlit Cloud, cargá estos valores en Settings > Secrets.

# ─── Supabase ────────────────────────────────────────────────────────────────
SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co"
SUPABASE_KEY = "eyJ..."   # anon public key
#
# Para generar un password_hash:
#   import hashlib; print(hashlib.sha256("tu_password".encode()).hexdigest())

[usuarios.admin]
usuario       = "admin"
password_hash = "HASH_SHA256_DE_TU_PASSWORD"
rol           = "admin"
turno         = ""

[usuarios.coord1]
usuario       = "coord1"
password_hash = "HASH_SHA256_DE_TU_PASSWORD"
rol           = "coordinadora"
turno         = "dia"

[usuarios.coord2]
usuario       = "coord2"
password_hash = "HASH_SHA256_DE_TU_PASSWORD"
rol           = "coordinadora"
turno         = "noche"
